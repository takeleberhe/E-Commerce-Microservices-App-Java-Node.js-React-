# Server Configuration
server:
  port: 8080
# Spring Application Configuration
spring:
  application:
    name: ConfigServer

  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations
